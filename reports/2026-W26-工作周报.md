# 智慧畜牧系统 · 工作周报

- **周期**：2026-06-19（周五）~ 2026-06-26（周五），第 26 周
- **来源**：基于 master 分支 git 提交记录梳理（共 50+ 次提交）
- **主线**：AI-Platform Phase A 落地合并 + datagen 数据合成轨道启动

---

## 一、AI-Platform Phase A：无监督健康异常检测微服务（本周最大产出，已合并 master）

本周主体工程，从设计到落地全程 TDD，最终 PR #62 squash 合并（commit `79c5fd22`，14 个 Task，67 测试通过）。

- **设计与评审（6/19–6/20）**：AI 健康路线图 + Phase A 设计评审补强 → 写出 Phase A Plan 1（Python 实施计划）→ 两轮评审共核出 17 条问题全部修复。
- **Task 1–5（6/20）**：项目骨架与依赖、API schemas 契约、时序对齐与 N_eff 计算、稳健基线（个体+群体兜底）、STL 残差与 10 维特征向量。
- **Task 6–10（6/20–6/21）**：L1a STL + L1b CUSUM 检测器、L1c Mahalanobis 联合检测器、融合归一化与加权、capability 契约 + N_eff 路由迟滞、L1 health 能力落地；其间修复 Mahalanobis LOOCV（消除 in-sample 偏差）、CUSUM+Mahalanobis 经验排名归一化、基线排除当前窗口防 self-leak 等正确性问题。
- **Task 11–14（6/21）**：engine 透传层、db.py 直连 PG 只读查询、FastAPI orchestration、docker-compose 集成 + 合成注入端到端测试。
- **评审修复 + 收尾（6/21–6/22）**：应用 code review 的 High/Medium/Low 全量修复；真实数据验证发现"部分维缺失 resample 崩溃"，改为优雅降级；归档 Phase A 评审报告与评审方法论教训。

## 二、datagen 限界上下文：数据合成与评估（6/26 集中推进的新方向）

为解决 AI-Platform 真实数据时间窗不齐（temp/mot/act 无交集、n_eff=0 全走规则档）的根因，新建 datagen 限界上下文，把"合成数据"升为一等公民。

- **路线图升级（6/26 上午）**：AI 路线图改为双轨制——datagen 横向数据轨道 × Phase 能力里程碑；Phase B/C 数据策略修订，Phase B 不再阻塞于 #55；标注基础设施 #56 从 Phase B 移至 Phase C。
- **设计与计划（6/26 上午）**：datagen 限界上下文设计规格 + datagen-v1 实施计划（12 task TDD）。
- **评审（6/26 中午）**：设计+计划评审出 2P0/6P1/4P2，全部修复（设计文档 + 实施计划 v2）。
- **GPS 定性纠偏（6/26 下午）**：撤销"GSP 三写冲突 P0"误报 → 重新定性真实问题归属 GPS 收敛设计 → 标记前置依赖已满足。
- **Task 1–3 落地（6/26 晚）**：V38 迁移（新增 `synthesis_scenarios` + `ground_truth_labels` 表 + 默认场景种子）；领域枚举 + `SynthesisScenario` + `GroundTruthLabel` 模型 + 单测（16 passed）。

## 三、后端 ranch/iot 修复与优化

- **6/24**：`FenceZoneJpaEntity.vertices` 加 `@ColumnTransformer` 修复 JSONB 写入（CI 根因）；`FenceZoneJourneyTest` 改用 `postRaw+assertCreated` 修 CI。两条均为 AI-Platform 合并后暴露的 CI 阻塞，修完才完成合并。
- **6/25**：修复"退出后再次登录围栏地图不显示"的用户可感知 bug。
- **6/26**：围栏检测优化 + Livestock save 的 `createdAt` 保留 + 构建部署调整。

## 四、前端 Flutter 清理与供应链安全

- **6/25**：统一 `activeFarmId` 写入入口（refactor 方向 4）；修预存 broken 测试 + arb 文案补单位。
- **6/26**：全项目 `dart fix prefer_const` 清理 + b2b tile 重构；`intl` 版本约束 `any → ^0.20.2`（供应链安全）。

## 五、基础设施

- **6/22**：Dockerfile `pg_hba.conf` 权限修复（chmod 644）。

---

## 本周结论

- **核心战果**：AI-Platform Phase A（无监督健康异常检测）从设计、评审、14 个 Task 的 TDD 实现到合并 master 全链路完成。
- **副产品**：真实数据验证暴露了数据时间窗不齐的根因，据此启动 datagen 数据合成轨道，并在本周内完成其设计、计划、评审和首批 Task 落地。
- **穿插工作**：修复若干后端 CI / 围栏 bug，以及前端供应链安全与代码清理。

---

> 数据指标均引用自 git commit message（作者自填），如需精确核对可对照 `git log --since="2026-06-19"`。
