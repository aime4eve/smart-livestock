# 工作产出周报

**报告周期**: 2026年7月20日 — 2026年7月24日
**提交**: 24 次（18 功能 + 6 合并） · +14,166 / -644 行 · 6 个 PR 合并 · build.number 迭代 12 次

---

# 一、功能建设

## 1. NIX-21: GPS 质量检验缺口补齐（PR #83, 7/20–7/21）

批量导入预览、动态对比、注册接线闭环。

| 层 | 产出物 |
|----|--------|
| 后端 | `DynamicQualityReportService`、`GpsQualityBatchImportService`（增强）、DTO `BatchParseResultDto`/`DynamicComparisonDto`、`GpsQualityAdminController` 预览+对比端点 |
| 前端 | `batch_import_dialog`（重写）、`comparison_tab`（增强）、`route_match_chart` 图表组件、`quality_check_list` 交互完善 |
| 测试 | `batch_import_preview_test`、`comparison_tab_test`、`gps_quality_models_test`、`quality_check_list_test`、`route_match_chart_test` |
| 文档 | `nix21-gap-closure-spec` + `nix21-gap-closure-plan` |

## 2. 设备软删除 + EUI 复活机制（PR #84, 7/21）

设备生命周期新增软删除，支持 EUI 重新激活，避免外键孤儿与平台状态错乱。

| 层 | 产出物 |
|----|--------|
| 后端 | `DeviceApplicationService`（软删除+复活）、`Device` 状态机、`DeviceRepository`/`SpringDataDeviceRepository`（过滤）、`DeviceController` 端点 |
| 前端 | 设备列表软删除 UI、`highfi_device_tile` 状态适配 |
| 数据库 | `V20260721100000` 软删除唯一索引、`V20260721101000` 遗留退役设备治理 |
| 测试 | `DeviceSoftDeleteJourneyTest`（集成）、`DeviceApplicationServiceTest`、`DeviceTest` |
| 文档 | `device-soft-delete-design` + `device-soft-delete-plan` + **6 篇评审**（design-review / re-review / final-review / legacy-governance-review / plan-review / plan-re-review）+ `blade-env-mapping-design` |

## 3. NIX-22: RTK 轨迹导入动态检验（PR #85 + #86, 7/22）

本周体量最大。新增 TRAJECTORY 检验类型，导入 RTK 真值轨迹并与实测轨迹配对生成质量报告；未注册设备自动注册。

| 层 | 产出物 |
|----|--------|
| 后端 | `TrajectoryImportService`（530 行，解析+自动注册）、`TrajectoryReportService`、`TrajectoryPairingService`（配对算法）、模型 `GpsQualityTrackPoint`/`TrackMatchSource`/配对 DTO、`GpsQualityAdminController` 导入/解析/报告端点 |
| 前端 | `trajectory_import_dialog`、`trajectory_report_panel`、`trajectory_chart`、`comparison_tab` 增强 |
| 数据库 | `V20260722100000` `trajectory_track_points` 表 |
| 测试 | `TrajectoryPairingServiceTest`、`GpsQualityBatchImportServiceTest`、`quality_check_list_test`（扩展） |
| 文档 | `nix22-rtk-trajectory-import-spec` + `plan` + 高保真原型 HTML |

## 4. Android / iOS 构建脚本（PR #89, 7/23）

移动端从纯 Web 扩展到原生构建。

- `build_android.sh`、`build_ios.sh` 一键构建脚本
- 完整 iOS 工程（Podfile、Runner.xcodeproj、AppIcon 全尺寸、LaunchScreen、macOS Podfile）
- 后端 `build.number` 版本同步机制

## 5. NIX-52: 告警 UI/UX 重设计（进行中，未提交）

全栈开发中，当前 70 个文件未提交。设计阶段已完成：

- 高保真原型 HTML（`nix-52-alert-ui-redesign-prototype`）
- spec + plan（`2026-07-23-nix-52-alert-ui-ux-redesign`）
- 开发中：后端 `AlertApplicationService`/`AlertRepository`，前端 alerts 模块全栈 + 设计令牌统一（`design-tokens.md`）

## 6. 其他功能产出

- blade 对接脚本 `devices.conf` 多轮维护
- `V20260724100000` 第二个 platform_admin seed

---

# 二、Bug 修复

| 问题 | 修复产出物 | 日期 |
|------|-----------|------|
| 健康率超过 100% — 软删除牲畜残留健康快照未过滤 | `HealthQueryPortAdapter` 过滤软删除 | 7/22 |
| 地图降级高德后 OSM 探针仍刷新，控制台 ERR_CONNECTION_RESET 刷屏 | `smart_tile_provider` 降级后停止 OSM 探针 | 7/22 |
| 地图首次探测前 OSM 超时导致反复刷新 | `smart_tile_provider` 首次探测前返回透明瓦片 | 7/22 |
| GPS 质量检验时间窗口未覆盖 blade 平台时区偏移 | `GpsQualityReportService`/`DynamicQualityReportService` 时间窗口对齐 | 7/22 |
| blade 同步容错 / 轨迹配对时间基准 / deviceCode 不统一 | `TrajectoryImportService` 容错 + 时间基准统一 | 7/23 |
| RocketMQ 遥测同步积压导致新设备饿死 | `AgenticPlatformTelemetrySyncJob` + `SyncDispatcher` 消除 backlog | 7/24 |
| `gps_logs` 重复写入（导入/同步重复轨迹） | `V20260720120000` 幂等 upsert + `(device_id, recorded_at)` 唯一约束去重 | 7/21 |

---

# 三、性能优化

## NIX-23: 牧场总览页加载优化（PR #88, 7/22–7/23）

| 层 | 产出物 |
|----|--------|
| 后端 | `RanchOverviewApplicationService` 消除重复查询 + 应用层缓存、`IoTQueryPortImpl` 查询去重 |
| 前端 | `ranch_page` 缓存 + 去重 + 预加载 + 骨架屏、`session_controller` 预取数据 |
| 基础设施 | nginx gzip + split debug info（首屏加速） |

---

# 统计摘要

| 指标 | 数值 |
|------|------|
| 提交 | 24（功能 18 + 合并 6） |
| 代码量 | +14,166 / -644 |
| PR 合并 | 6（#83 #84 #85 #86 #88 #89） |
| Flyway 迁移 | 5 |
| 新增测试文件 | 9 |
| 设计文档 | 13 篇（spec 4 + plan 3 + review 6） |
| 高保真原型 | 2 |
| build.number 迭代 | 12 |

| 类别 | 产出规模 |
|------|---------|
| 功能建设 | 5 个交付模块 + 1 个进行中，约 13,000 行新增 |
| Bug 修复 | 7 项（健康率、地图瓦片 ×2、时区、同步容错、MQ 积压、gps_logs 去重） |
| 性能优化 | NIX-23 全栈优化 + nginx 加速 |

---

*基于 git 记录与工作区状态整理 · 2026年7月24日*
