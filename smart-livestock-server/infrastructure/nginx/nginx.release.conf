# =============================================================================
# Smart Livestock — release nginx baseline (TLS termination)
# Task card: NIX-184 T8a
# Differences vs dev/test infrastructure/nginx/nginx.conf:
#   - Port 80 only issues a global 301 redirect to HTTPS.
#   - Port 443 terminates TLS (TLSv1.2/1.3) with certs bind-mounted from
#     the release package ./secrets/certs/ directory.
#   - gzip lifted to the shared http-level context (applies to both servers).
# =============================================================================

# ── Gzip compression: main.dart.js (5MB→~1.2MB), canvaskit.wasm (7MB→~2.5MB) ──
gzip on;
gzip_min_length 1024;
gzip_comp_level 6;
gzip_types text/plain text/css text/javascript application/javascript application/json application/wasm;
gzip_vary on;

# ── HTTP :80 → HTTPS global redirect ──
server {
    listen 80;
    server_name _;

    # Assumes HTTPS on the standard port 443. If HTTPS_PORT is customized in
    # .env.release, adjust the redirect target here (this file is baked into
    # the image at build time and does not read compose env vars).
    return 301 https://$host$request_uri;
}

# ── TLS termination + reverse proxy ──
server {
    listen 443 ssl;
    server_name _;

    # Certificates are bind-mounted from the release package ./secrets/certs/.
    ssl_certificate     /etc/nginx/certs/fullchain.pem;
    ssl_certificate_key /etc/nginx/certs/privkey.pem;

    # Basic TLS hardening (kept conservative on purpose).
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers off;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 1d;
    ssl_session_tickets off;

    # ── Tile proxy: 404 passthrough — 前端 SmartTileProvider 据此降级（原设计 §3.1）──
    location /tiles/ {
        proxy_pass http://tileserver:8080/data/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        add_header Cache-Control "public, max-age=2592000";
    }

    location /api/v1/ {
        proxy_pass http://app:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /health {
        proxy_pass http://app:8080/health;
    }

    # Cache-bust main.dart.js — Flutter web entry point changes every build
    location = /main.dart.js {
        root /usr/share/nginx/html;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        add_header Pragma "no-cache";
    }

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri /index.html;
    }
}
