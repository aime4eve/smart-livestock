# Release nginx image (NIX-184 T8a).
# Build context = smart-livestock-server repo root, same contract as the dev/test
# infrastructure/nginx/Dockerfile, so scripts/build-release-package.sh (T8b) can reuse it:
#   docker build -f infrastructure/nginx/Dockerfile.release -t smart-livestock/nginx:${RELEASE_VERSION} .
# frontend/ is the build_web.sh output copied into the packaging build context by T8b.
FROM nginx:alpine
COPY infrastructure/nginx/nginx.release.conf /etc/nginx/conf.d/default.conf
COPY infrastructure/nginx/empty.png /usr/share/nginx/html/empty.png
COPY frontend /usr/share/nginx/html
RUN chmod -R a+rX /usr/share/nginx/html
