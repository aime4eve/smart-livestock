# ============================================
# Smart Livestock Server - DEV Environment Config
# Copy to .env.dev and fill with real values
# Generate keys: openssl rand -base64 48
# ============================================

# Database
DB_HOST=postgres
DB_PORT=5432
DB_NAME=smart_livestock
DB_USER=postgres
DB_PASSWORD=your-secure-dev-password

# Redis
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=

# RocketMQ
ROCKETMQ_NAME_SERVER=rocketmq-namesrv:9876

# JWT — MUST be different from test environment
JWT_SECRET=generate-with-openssl-rand-base64-48
JWT_ACCESS_EXPIRATION=3600000
JWT_REFRESH_EXPIRATION=604800000

# Server
SERVER_PORT=8080

# Device Simulator API Keys — MUST be different from test environment
SIMULATOR_DEVICE_REGISTER_KEY=generate-unique-key
SIMULATOR_FENCE_SYNC_KEY=generate-unique-key

# blade 平台（第三方 IoT 平台）— dev 环境
# test 环境地址见 .env.example；设计文档:
# docs/superpowers/specs/2026-07-21-blade-env-mapping-design.md
AGENTIC_PLATFORM_DEVICE_BASE_URL=http://172.21.2.41:8100
AGENTIC_PLATFORM_LICENSE_BASE_URL=http://172.21.2.41:8100
AGENTIC_PLATFORM_OAUTH2_TOKEN_URI=http://172.21.2.41:8108/oauth2/token
AGENTIC_PLATFORM_OAUTH2_ENABLED=false
AGENTIC_PLATFORM_OAUTH2_CLIENT_ID=hkt_openapi
AGENTIC_PLATFORM_OAUTH2_CLIENT_SECRET=your-blade-client-secret
AGENTIC_PLATFORM_OAUTH2_SERVICE_USER_ID=your-service-user-id
AGENTIC_PLATFORM_TENANT_ID=000000
