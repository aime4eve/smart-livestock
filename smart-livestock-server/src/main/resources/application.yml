server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:smart_livestock}?stringtype=unspecified
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:postgres}
    hikari:
      maximum-pool-size: ${DB_POOL_MAX:50}
      minimum-idle: 10
      connection-timeout: 30000
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration
    out-of-order: true
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
rocketmq:
  name-server: ${ROCKETMQ_NAME_SERVER:localhost:9876}
  producer:
    group: ${ROCKETMQ_PRODUCER_GROUP:smart-livestock-producer}

jwt:
  secret: ${JWT_SECRET:default-secret-change-in-production}
  access-expiration: ${JWT_ACCESS_EXPIRATION:3600000}
  refresh-expiration: ${JWT_REFRESH_EXPIRATION:604800000}

telemetry:
  simulator:
    enabled: ${TELEMETRY_SIMULATOR_ENABLED:false}
    interval-ms: ${TELEMETRY_SIMULATOR_INTERVAL_MS:30000}

datagen:
  enabled: ${DATAGEN_ENABLED:true}
  tick-ms: ${DATAGEN_TICK_MS:10000}
  retention-days: ${DATAGEN_RETENTION_DAYS:30}
  retention-cron: ${DATAGEN_RETENTION_CRON:0 0 3 * * *}

ai:
  platform:
    url: ${AI_PLATFORM_URL:http://localhost:18000}
    timeout-ms: ${AI_PLATFORM_TIMEOUT_MS:5000}
  alert:
    threshold: ${AI_ALERT_THRESHOLD:0.7}
  dedup:
    ttl-minutes: ${AI_DEDUP_TTL_MINUTES:60}

# Agentic-middle-platform (blade) integration — OAuth2 + Feign url mode
agentic-platform:
  device:
    base-url: ${AGENTIC_PLATFORM_DEVICE_BASE_URL:http://172.22.4.17:8100}
  license:
    base-url: ${AGENTIC_PLATFORM_LICENSE_BASE_URL:http://172.22.4.17:8100}
  oauth2:
    enabled: ${AGENTIC_PLATFORM_OAUTH2_ENABLED:false}
    token-uri: ${AGENTIC_PLATFORM_OAUTH2_TOKEN_URI:http://172.22.4.17:8108/oauth2/token}
    client-id: ${AGENTIC_PLATFORM_OAUTH2_CLIENT_ID:hkt_openapi}
    client-secret: ${AGENTIC_PLATFORM_OAUTH2_CLIENT_SECRET:RLuXd5H8RkZZRPA6TKbf72XmjKYNq}
    service-user-id: ${AGENTIC_PLATFORM_OAUTH2_SERVICE_USER_ID:2074385063398711296}
    tenant-id: ${AGENTIC_PLATFORM_TENANT_ID:000000}
  feign-auth:
    header-name: ${AGENTIC_PLATFORM_FEIGN_HEADER_NAME:token}
    token-prefix: ${AGENTIC_PLATFORM_FEIGN_TOKEN_PREFIX:}
  service-account:
    tenant-id: ${AGENTIC_PLATFORM_SERVICE_TENANT_ID:000000}
  sync:
    enabled: ${AGENTIC_PLATFORM_SYNC_ENABLED:false}
    dispatch-interval-ms: ${AGENTIC_PLATFORM_SYNC_INTERVAL_MS:300000}
