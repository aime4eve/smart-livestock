server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:smart_livestock}?stringtype=unspecified
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:postgres}
    hikari:
      maximum-pool-size: ${DB_POOL_MAX:50}
      minimum-idle: 10
      connection-timeout: 30000
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration
    out-of-order: true
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
rocketmq:
  name-server: ${ROCKETMQ_NAME_SERVER:localhost:9876}
  producer:
    group: ${ROCKETMQ_PRODUCER_GROUP:smart-livestock-producer}

jwt:
  secret: ${JWT_SECRET:default-secret-change-in-production}
  access-expiration: ${JWT_ACCESS_EXPIRATION:3600000}
  refresh-expiration: ${JWT_REFRESH_EXPIRATION:604800000}

telemetry:
  simulator:
    enabled: ${TELEMETRY_SIMULATOR_ENABLED:false}
    interval-ms: ${TELEMETRY_SIMULATOR_INTERVAL_MS:30000}

gps:
  ingestion:
    poll-ms: ${GPS_INGESTION_POLL_MS:500}
    batch-size: ${GPS_INGESTION_BATCH_SIZE:100}
    max-attempts: ${GPS_INGESTION_MAX_ATTEMPTS:10}
    retry-delay: ${GPS_INGESTION_RETRY_DELAY:30s}

datagen:
  enabled: ${DATAGEN_ENABLED:true}
  tick-ms: ${DATAGEN_TICK_MS:10000}
  retention-days: ${DATAGEN_RETENTION_DAYS:30}
  retention-cron: ${DATAGEN_RETENTION_CRON:0 0 3 * * *}

platform:
  partition:
    months-ahead: ${PARTITION_MONTHS_AHEAD:2}
    check-cron: ${PARTITION_CHECK_CRON:0 10 2 * * *}

ai:
  platform:
    url: ${AI_PLATFORM_URL:http://localhost:18000}
    timeout-ms: ${AI_PLATFORM_TIMEOUT_MS:5000}
  alert:
    threshold: ${AI_ALERT_THRESHOLD:0.7}
  dedup:
    ttl-minutes: ${AI_DEDUP_TTL_MINUTES:60}

# Agentic-middle-platform (blade) integration — OAuth2 + Feign url mode
agentic-platform:
  device:
    base-url: ${AGENTIC_PLATFORM_DEVICE_BASE_URL:http://172.22.4.17:8100}
  license:
    base-url: ${AGENTIC_PLATFORM_LICENSE_BASE_URL:http://172.22.4.17:8100}
  oauth2:
    enabled: ${AGENTIC_PLATFORM_OAUTH2_ENABLED:false}
    token-uri: ${AGENTIC_PLATFORM_OAUTH2_TOKEN_URI:http://172.22.4.17:8108/oauth2/token}
    client-id: ${AGENTIC_PLATFORM_OAUTH2_CLIENT_ID:hkt_openapi}
    client-secret: ${AGENTIC_PLATFORM_OAUTH2_CLIENT_SECRET:RLuXd5H8RkZZRPA6TKbf72XmjKYNq}
    service-user-id: ${AGENTIC_PLATFORM_OAUTH2_SERVICE_USER_ID:2074385063398711296}
    tenant-id: ${AGENTIC_PLATFORM_TENANT_ID:000000}
  feign-auth:
    header-name: ${AGENTIC_PLATFORM_FEIGN_HEADER_NAME:token}
    token-prefix: ${AGENTIC_PLATFORM_FEIGN_TOKEN_PREFIX:}
  service-account:
    tenant-id: ${AGENTIC_PLATFORM_SERVICE_TENANT_ID:000000}
  sync:
    enabled: ${AGENTIC_PLATFORM_SYNC_ENABLED:false}
    dispatch-interval-ms: ${AGENTIC_PLATFORM_SYNC_INTERVAL_MS:300000}

# ThingsBoard direct telemetry channel (NIX-179, disabled by default)
smartlivestock:
  ns:
    enabled: ${SMARTLIVESTOCK_NS_ENABLED:false}
    base-url: ${SMARTLIVESTOCK_NS_BASE_URL:http://172.17.201.15}
    username: ${SMARTLIVESTOCK_NS_USERNAME:}
    password: ${SMARTLIVESTOCK_NS_PASSWORD:}
    org-id: ${SMARTLIVESTOCK_NS_ORG_ID:1}
    page-size: ${SMARTLIVESTOCK_NS_PAGE_SIZE:100}
  tb:
    enabled: ${SMARTLIVESTOCK_TB_ENABLED:false}
    base-url: ${SMARTLIVESTOCK_TB_BASE_URL:http://172.22.3.105}
    username: ${SMARTLIVESTOCK_TB_USERNAME:tenant@hkt.com}
    password: ${SMARTLIVESTOCK_TB_PASSWORD:}
    poll-interval-ms: ${SMARTLIVESTOCK_TB_POLL_INTERVAL_MS:300000}
    lookback-days: ${SMARTLIVESTOCK_TB_LOOKBACK_DAYS:7}
    batch-size: ${SMARTLIVESTOCK_TB_BATCH_SIZE:200}
    blade-exclusion: ${SMARTLIVESTOCK_TB_BLADE_EXCLUSION:false}
    tenant-id: ${SMARTLIVESTOCK_TB_TENANT_ID:1}
    fallback:
      # Blade fallback triggers: TB cursor frozen longer than this window, or
      # this many consecutive failed cycles, degrades the device to blade.
      stale-after-ms: ${SMARTLIVESTOCK_TB_FALLBACK_STALE_AFTER_MS:900000}
      failure-threshold: ${SMARTLIVESTOCK_TB_FALLBACK_FAILURE_THRESHOLD:3}
  # Licensing context (NIX-184): HOSTED = we operate the server, ONPREM =
  # customer-owned server with offline license enforcement.
  license:
    mode: ${SMARTLIVESTOCK_LICENSE_MODE:HOSTED}
    public-key-file: ${SMARTLIVESTOCK_LICENSE_PUBLIC_KEY_FILE:classpath:licensing/license-public-keys.json}
    time-tolerance: ${SMARTLIVESTOCK_LICENSE_TIME_TOLERANCE:PT2M}
    validation-cron: ${SMARTLIVESTOCK_LICENSE_VALIDATION_CRON:0 */5 * * * *}
    host-fingerprint-file: ${SMARTLIVESTOCK_LICENSE_HOST_FINGERPRINT_FILE:/etc/machine-id}
  # Hosted pilot-license API toggle (consumed by the licensing application layer).
  pilot-license:
    enabled: ${SMARTLIVESTOCK_PILOT_LICENSE_ENABLED:true}
