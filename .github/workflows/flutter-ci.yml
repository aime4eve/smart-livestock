name: Flutter CI

on:
  push:
    branches: [master]
    paths:
      - 'Mobile/mobile_app/**'
  pull_request:
    branches: [master]
    paths:
      - 'Mobile/mobile_app/**'

defaults:
  run:
    working-directory: Mobile/mobile_app

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          # Pinned to the version used on dev machines: newer framework
          # releases add widget assertions (e.g. ListTile-covered-by-Material)
          # that fail tests before local runs catch up.
          flutter-version: '3.41.5'
          channel: 'stable'
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Analyze
        run: flutter analyze --no-fatal-infos --no-fatal-warnings

      - name: Run tests
        run: find test -name '*_test.dart' -not -path 'test/e2e/*' | xargs flutter test --exclude-tags=golden
