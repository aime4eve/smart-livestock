name: Flutter CI

on:
  push:
    branches: [master]
    paths:
      - 'Mobile/mobile_app/**'
  pull_request:
    branches: [master]
    paths:
      - 'Mobile/mobile_app/**'

defaults:
  run:
    working-directory: Mobile/mobile_app

jobs:
  test:
    runs-on: ubuntu-latest

 steps:
      - uses: actions/checkout@v5

     - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Analyze
        run: flutter analyze --no-fatal-infos --no-fatal-warnings

      - name: Run tests
        run: find test -name '*_test.dart' -not -path 'test/e2e/*' | xargs flutter test
