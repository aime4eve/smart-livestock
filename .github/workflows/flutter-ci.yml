name: Flutter CI

on:
  push:
    branches: [master]
    paths:
      - 'Mobile/mobile_app/**'
  pull_request:
    branches: [master]
    paths:
      - 'Mobile/mobile_app/**'

defaults:
  run:
    working-directory: Mobile/mobile_app

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: 'stable'
          channel: 'stable'
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Analyze
        run: flutter analyze --no-fatal-infos

      - name: Run tests
        run: flutter test
