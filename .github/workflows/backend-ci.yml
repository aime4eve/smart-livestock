name: Backend CI

on:
  push:
    branches: [master]
    paths:
      - 'smart-livestock-server/**'
  pull_request:
    branches: [master]
    paths:
      - 'smart-livestock-server/**'

defaults:
  run:
    working-directory: smart-livestock-server

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Cache Gradle packages
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ hashFiles('smart-livestock-server/**/*.gradle*', 'smart-livestock-server/**/gradle-wrapper.properties') }}
          restore-keys: gradle-

      - name: Compile
        run: ./gradlew compileJava

      - name: Run tests
        run: ./gradlew test
